# =============================================================================
# Production Requirements — excludes torch/transformers (not used by web service)
# The web service uses OpenAI embeddings (EMBEDDING_PROVIDER=openai)
# torch/sentence-transformers are only needed for offline ingestion pipelines
# =============================================================================

# Core
fastapi==0.115.6
slowapi>=0.1.9
python-multipart>=0.0.9
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1

# AI/ML — LangChain + LangGraph
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langgraph>=0.1.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0

# Utilities
python-dotenv==1.0.0

# OpenAI
openai>=1.10.0
langchain-openai>=0.3.0
tiktoken>=0.5.2

# Vector Database
qdrant-client>=1.7.0

# Numerics (required by qdrant-client and langchain internals)
scikit-learn>=1.4.0
numpy>=1.26.0

# Agent Tools
httpx>=0.27.0
sendgrid>=6.11.0
aiosmtplib>=3.0.0
redis>=5.0.0

# Google Calendar
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0

# Resilience
tenacity>=8.2.0

# Analytics & Monitoring
prometheus-fastapi-instrumentator>=7.0.0
